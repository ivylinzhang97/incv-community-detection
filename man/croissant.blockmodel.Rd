% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/croissant.R
\name{croissant.blockmodel}
\alias{croissant.blockmodel}
\title{CROISSANT for blockmodel selection}
\usage{
croissant.blockmodel(
  A,
  K.CAND,
  s,
  o,
  R,
  tau = 0,
  laplace = FALSE,
  dc.est = 2,
  loss = c("l2", "bin.dev", "AUC"),
  ncore = 1
)
}
\arguments{
\item{A}{Adjacency matrix (n x n, can be sparse).}

\item{K.CAND}{Candidate community numbers (integer vector, or a single
integer interpreted as \code{1:K.CAND}).}

\item{s}{Number of non-overlapping subsamples.}

\item{o}{Size of the overlapping set.}

\item{R}{Number of independent repetitions.}

\item{tau}{Regularisation parameter for the adjacency (default 0).}

\item{laplace}{Logical; use graph Laplacian normalisation (default FALSE).}

\item{dc.est}{DCBM estimation method: 1 = eigenvector, 2 = fast (default 2).}

\item{loss}{Character vector of loss functions to evaluate.
Supported: \code{"l2"}, \code{"bin.dev"}, \code{"AUC"}.}

\item{ncore}{Number of cores for parallel computation.}
}
\value{
A list containing:
  \item{loss}{data.table of loss values by candidate model and K.}
  \item{Candidate Models}{"SBM and DCBM".}
  \item{Candidate Values}{The candidate K vector.}
  \item{*.model}{Selected model string (e.g. "SBM-3") for each loss.}
}
\description{
Cross-validated, Overlapping, In-Sample Selection of the Number of
communities and model Type. Jointly selects between SBM and DCBM and
the number of communities \code{K} using overlapping node subsamples.
}
