% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{community.sim}
\alias{community.sim}
\title{Simulate a Stochastic Block Model network}
\usage{
community.sim(k = 2, n = 1000, n1 = 100, p = 0.3, q = 0.1)
}
\arguments{
\item{k}{Number of communities (default 2).}

\item{n}{Total number of nodes (default 1000).}

\item{n1}{Size of the smallest community (default 100).}

\item{p}{Within-community connection probability (default 0.3).}

\item{q}{Between-community connection probability (default 0.1).}
}
\value{
A list with:
  \item{membership}{Integer vector of community labels (length \code{n}).}
  \item{adjacency}{n x n binary symmetric adjacency matrix.}
}
\description{
Generates an adjacency matrix from a planted-partition SBM with \code{k}
communities. The first community has size \code{n1}; the remaining
communities share the leftover nodes roughly equally.
}
\examples{
net <- community.sim(k = 3, n = 120, n1 = 30, p = 0.5, q = 0.1)
table(net$membership)
}
